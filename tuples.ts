let user: [string, number, boolean];

// user = [true, "John Doe", 1]; // This is not allowed because the order of the values is not correct

user = ["John Doe", 1, true];


let rgb: [number, number, number] = [255, 0, 0];


type Employee = [string, number];

export {};