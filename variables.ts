let greetings: string = "Hello, World!";

// greetings = 10; // This will throw an error because greetings is a string


let mynum = 10;

// mynum.toUpperCase(); // This will throw an error because mynum is a number


// number type
// ###########

let userId: number = 334455;

// boolean type
// ############

let isLoggedIn: boolean = true;

// ** Note: Sometimes it's unessesary to specify the type of a variable

export {}; // This is to avoid TS error: "Cannot redeclare block-scoped variable 'greetings'."